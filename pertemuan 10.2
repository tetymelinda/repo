#include <ncurses\curses.h>
#include <windows.h>
using namespace std;

int main() {
    system("color C5");
    initscr();

    mvprintw(2, 2, "A");
    refresh();
    Sleep(500); // delay 0.5 detik

    mvprintw(3, 2, "B");
    refresh();
    Sleep(1000); // delay 1 detik

    mvprintw(4, 2, "D");
    refresh();
    Sleep(1500); // delay 1.5 detik

    mvprintw(5, 2, "U");
    refresh();
    Sleep(500); // delay 0.5 detik

    mvprintw(6, 2, "L");
    refresh();

    getch();
    endwin();

    return 0;
}
